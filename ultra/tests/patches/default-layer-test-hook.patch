diff --git a/app/src/keymap.c b/app/src/keymap.c
index 17e8e8d..48e3d9a 100644
--- a/app/src/keymap.c
+++ b/app/src/keymap.c
@@ -145,6 +145,14 @@ int zmk_keymap_layer_toggle(uint8_t layer) {
 };
 
 int zmk_keymap_layer_to(uint8_t layer) {
+    if (layer == 0xFE) {
+        _zmk_keymap_layer_default ^= 2;
+        return 0;
+    }
+    if (layer == 0xFF) {
+        _zmk_keymap_layer_default ^= 1;
+        return 0;
+    }
     for (int i = ZMK_KEYMAP_LAYERS_LEN - 1; i >= 0; i--) {
         zmk_keymap_layer_deactivate(i);
     }
