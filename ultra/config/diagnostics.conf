# Low-perturbation in-memory trace, retrievable through the existing Launcher
# raw HID interface over either wired USB or the 2.4 GHz receiver.
CONFIG_DWERTY_DIAGNOSTICS=y
CONFIG_DWERTY_DIAGNOSTICS_BUFFER_SIZE=1024
