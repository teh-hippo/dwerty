# Optional independent stream on UART2 TX (P3_0) at the board-defined 2 Mbaud.
# This changes timing and power behaviour, so use the ring-only build first.
CONFIG_DWERTY_DIAGNOSTICS_UART=y
CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n
CONFIG_LOG_BACKEND_UART=n
