# Overrides appended to the stock keychron_v6_ultra_ansi.conf at build time.
# The stock .conf holds the essential SoC/BT/DFU settings, so we append rather
# than replace.
#
# No overrides are needed for the DWERTY spike. NKRO was attempted here but the
# fork's HID report-type selection rejects a bare
# CONFIG_ZMK_HID_REPORT_TYPE_NKRO=y (hid.h: "A proper HID report type must be
# selected"); revisit if full N-key rollover parity is wanted.
